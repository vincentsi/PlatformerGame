#pragma once

namespace Physics {
    constexpr float GRAVITY = 980.0f;           // pixels/s²
    constexpr float TERMINAL_VELOCITY = 1000.0f; // max fall speed
    constexpr float EPSILON = 0.01f;            // small value for collision resolution
}
